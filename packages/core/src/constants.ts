/**
 * Constants used throughout the component library
 */

export const COMPONENT_PREFIX = 'vwc';

// Component names
export const COMPONENT_NAMES = {
  BUTTON: `${COMPONENT_PREFIX}-button`,
  INPUT: `${COMPONENT_PREFIX}-input`,
  ICON: `${COMPONENT_PREFIX}-icon`,
};

// CSS Variable prefix
export const CSS_VAR_PREFIX = '--vwc';